import numpy as np
import cv2
import serial
import time

ser = serial.Serial('/dev/ttyUSB0', baudrate=115200)
time.sleep(0.5) #wait 0.5s for serial connection

# cw = [0xFF, 0x00, 0x00, 0x53, 0x00, 0x00] # 0x64
# ser.write(serial.to_bytes(cw))
# print(serial.to_bytes(cw))

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x02] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(3)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x04] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))


cw = [0xFF, 0x00, 0x32, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(1.5)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x03] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(0.5)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x01] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(3)

cw = [0xFF, 0x00, 0xBC, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(3)

cw = [0xFF, 0xBC, 0x00, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(5.5)

# cw = [0xFF, 0x00, 0x00, 0xAC, 0x00, 0x00] # 0x64
# ser.write(serial.to_bytes(cw))
# print(serial.to_bytes(cw))
# time.sleep(2)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x02] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(3.5)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x04] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))
time.sleep(3)

cw = [0xFF, 0x00, 0x00, 0x00, 0x00, 0x00] # 0x64
ser.write(serial.to_bytes(cw))
print(serial.to_bytes(cw))

